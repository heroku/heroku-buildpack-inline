#!/bin/sh
# Usage: bin/release <build-dir>

RELEASE_SCRIPT="${1}/${INLINE_BUILDPACK_PATH:-bin}/release"

# The Buildpack API states that the `bin/release` script is optional.
exec "${RELEASE_SCRIPT}" "$@"
